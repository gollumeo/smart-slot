includes:
    - vendor/larastan/larastan/extension.neon
    - vendor/nesbot/carbon/extension.neon

parameters:
    level: max
    excludePaths:
        - tests/**/*.php # ignoring tests files because of Pest automagic injection of $this
        - config/*.php
